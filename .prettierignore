# Dependencies
node_modules

# Ponder generated
.ponder
generated
ponder-env.d.ts

# Generated / vendored ABIs (never hand-edited)
abis

# Lockfile
pnpm-lock.yaml

# Vendored / generated Solidity stack — forge-fhevm submodule + the SDK reference
# deploy (copied verbatim, never hand-edited). Its packages declare a prettier
# plugin we don't install, so never let prettier descend here.
contracts
